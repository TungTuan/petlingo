<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
	<string>Petlingo</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsArbitraryLoads</key>
		<true/>
	</dict>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>armv7</string>
	</array>
	<!-- Landscape-only: the whole app is a fixed 1194×834 landscape canvas
	     (see index.css's .device-frame) — locking orientation here means iOS
	     itself never rotates to portrait, so the CSS rotate-90 workaround for
	     portrait-held phones never has to kick in on native builds. -->
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UIViewControllerBasedStatusBarAppearance</key>
	<true/>
	<!-- Required by iOS when an app doesn't support every orientation (we're
	     landscape-only, see UISupportedInterfaceOrientations above) — without
	     this, iPad Split View could still try to present the app in an
	     unsupported orientation. -->
	<key>UIRequiresFullScreen</key>
	<true/>
	<!-- Required by @capacitor-community/speech-recognition (EchoParrot.tsx,
	     "Vẹt Con Tập Nói") — iOS shows these exact strings in its permission
	     prompt, so they need to read naturally as a sentence, not a label. -->
	<key>NSSpeechRecognitionUsageDescription</key>
	<string>Petlingo cần quyền này để nghe bé nói và chấm điểm phát âm trong trò "Vẹt Con Tập Nói".</string>
	<key>NSMicrophoneUsageDescription</key>
	<string>Petlingo cần dùng micro để nghe bé tập nói tiếng Anh trong trò "Vẹt Con Tập Nói".</string>
</dict>
</plist>
